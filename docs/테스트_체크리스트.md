# BookShelf — 테스트 & 성능 체크리스트

> **최종 업데이트**: 2026-05-31  
> **환경**: 로컬 개발 서버 (`http://localhost:8787`)

### 최신 반영 메모 (2026-05-31)

- `WishlistPage` ISBN 스캐너 버튼 연동 반영
- 전역 뷰포트 리팩토링(`useViewport`, `svh`, safe-area 변수) 반영
- iOS Safari/노치 기기에서 탭바 겹침/잘림 회귀 테스트 항목 보강

---

## 실행 방법

```bash
# 정적 검증
npm run type-check
npm run lint

# 프론트엔드 빌드 + Worker 실행
npm run build
npx wrangler dev

# 브라우저에서 http://localhost:8787 접속
# DevTools 열기: F12 → Network, Application, Lighthouse 탭
```

---

## 1. E2E API 자동 테스트

```bash
bash scripts/e2e-api-test.sh
# 목표: 27/27 PASS
```

최근 실행(2026-04-28): ✅ 27/27 PASS

### 1-2. 관리자 API 교차검증

```bash
# 기본 실행(관리자 계정 필요)
bash scripts/admin-api-test.sh

# 관리자 JWT가 있으면 로그인 단계 생략
ADMIN_TOKEN="<admin-jwt>" bash scripts/admin-api-test.sh
```

참고:

- 기본 관리자 계정/비밀번호가 환경과 불일치하면 스크립트는 로그인 단계에서 종료될 수 있습니다.
- 2026-04-28 리팩토링으로 `ADMIN_TOKEN` 직접 주입 경로를 지원합니다.

---

## 2. 통합 기능 체크리스트

### 2-1. 신규 사용자 가입 플로우

- [ ] `http://localhost:8787` 접속 → `/splash` 리다이렉트 확인
- [ ] "시작하기" 클릭 → `/onboarding` 이동
- [ ] 3단계 슬라이드 (소개 → 장르 선택 → 독서 목표) 진행
- [ ] 장르 1개 이상 선택 후 완료 → `PATCH /api/users/profile` Network 요청 확인
- [ ] 이름·이메일·비밀번호 입력 → `POST /api/users/register` 요청 확인
- [ ] 성공 → DevTools Application > Local Storage에 `auth_token` 저장 확인
- [ ] `/library` (루트 `/`) 이동 확인

### 2-2. 로그아웃 & 재로그인

- [ ] 로그아웃 → Local Storage에서 `auth_token` 제거 확인
- [ ] `/login` 이동 확인
- [ ] 이메일/비밀번호 입력 → `POST /api/users/login` 확인
- [ ] 성공 → `/library` 이동 확인

### 2-3. ProtectedRoute 가드

- [ ] 미인증 상태에서 `/` 직접 접속 → `/login` 리다이렉트
- [ ] 로그인 후 동일 URL 접속 → 정상 접근
- [ ] `/register-flow`, `/stats`, `/groups` 등 보호 라우트 동일 확인

### 2-4. 책 등록 (OCR 경로)

- [ ] `+` 버튼 → RegisterFlowPage 진입
- [ ] 카메라 OCR 탭 → 사진 촬영/업로드 → `POST /api/ai/ocr` Network 요청 확인
- [ ] 신뢰도(confidence) % 표시 확인
- [ ] 결과 반영 후 Step 2로 이동 + 제목·저자 자동 입력 확인
- [ ] Step 2에서 pageCount "웹에서 가져오기" → `GET /api/search/pagecount` 요청 확인
- [ ] 등록 완료 → `POST /api/books` 요청 확인
- [ ] 서재 목록 즉시 갱신 확인

### 2-5. 책 등록 (ISBN 바코드 경로)

- [ ] ISBN 스캐너 탭 → 카메라 권한 허용 → 책 뒷면 스캔
- [ ] `GET /api/search/books/isbn?isbn=...` 요청 확인
- [ ] 정보 자동 완성 확인

### 2-5-1. 위시리스트 ISBN 스캔 경로 (신규)

- [ ] `/wishlist` 진입 → 검색 패널 오픈
- [ ] 검색 헤더의 스캔 버튼(`ScanLine`) 노출 확인
- [ ] 카메라 권한 허용 후 바코드 스캔 → 결과가 위시리스트에 즉시 추가되는지 확인
- [ ] 추가 성공 토스트 노출 및 검색 패널 닫힘 확인

### 2-6. 읽는 중 관리

- [ ] 현재 페이지 업데이트 → `POST /api/sessions` 요청 확인
- [ ] 목표일 기반 일일 목표 페이지 계산 표시
- [ ] 목표 대비 뒤처짐 → 빨간 경고 표시
- [ ] 독서 타이머 시작/정지/기록 동작 확인

### 2-7. 노트 & 검색

- [ ] 책 상세 → 노트 추가 → `POST /api/notes` 요청 확인
- [ ] 노트 검색 → `GET /api/notes?q=키워드` FTS5 결과 확인
- [ ] AI 독후감 보조 → `POST /api/ai/summarize` 요청 확인

### 2-8. 통계 & 결산

- [ ] `/stats` 접속 → `GET /api/stats` 요청 확인
- [ ] 월별 차트, 장르 도넛, 독서 히트맵 렌더링 확인
- [ ] `/yearly-review` 접속 → 연간 결산 데이터 확인

### 2-9. 독서 모임

- [ ] 그룹 생성 → `POST /api/groups` 요청 확인
- [ ] 가입 신청 → 리더 승인 → 채팅 접근 확인
- [ ] 일정 등록 (모든 멤버), 하루 2개 초과 시 에러 확인
- [ ] 알림 패널 → 서버 폴링(30초) 동작 확인

### 2-10. 오프라인 배너

- [ ] DevTools → Network → Offline → amber 배너 표시 확인
- [ ] 온라인 복귀 → 배너 자동 숨김 확인

---

## 3. 번들 크기 기준

```bash
# 빌드 후 청크별 크기 확인
npm run build 2>&1 | grep "dist/assets"
```

| 청크 | 허용 기준 |
|------|----------|
| `vendor-react-*.js` (gzip) | < 100 kB |
| `index-*.js` (앱 번들, gzip) | < 80 kB |
| `vendor-charts-*.js` (lazy, gzip) | < 120 kB |
| `index-*.css` (gzip) | < 25 kB |
| **초기 로드 합계** | **< 600 kB gzip** |

> `vendor-charts`는 `/stats` 진입 시에만 로드되는 lazy chunk

### 3-1. 최근 측정값 (2026-04-28 build)

| 청크 | 측정값 (gzip) | 기준 대비 |
|------|---------------|-----------|
| `vendor-react-*.js` | 102.77 kB | ⚠️ +2.77 kB 초과 |
| `index-*.js` | 31.21 kB | ✅ 기준 이내 |
| `vendor-charts-*.js` | 102.40 kB | ✅ 기준 이내 |
| `index-*.css` | 17.82 kB | ✅ 기준 이내 |

---

## 4. Lighthouse 목표 점수

1. `http://localhost:8787` 시크릿 창에서 접속
2. DevTools (`F12`) → **Lighthouse** 탭
3. Device: **Mobile** (우선 측정)
4. Categories: Performance, Accessibility, Best Practices, SEO, PWA 모두 체크
5. **Analyze page load** 클릭

| 항목 | 목표 점수 | 결과 | 판정 |
|------|----------|------|------|
| Performance | ≥ 80 | | |
| Accessibility | ≥ 90 | | |
| Best Practices | ≥ 90 | | |
| SEO | ≥ 80 | | |
| PWA | ≥ 90 | | |

---

## 5. Core Web Vitals 기준

| 지표 | 설명 | 목표 |
|------|------|------|
| LCP | Largest Contentful Paint | < 2.5초 |
| CLS | Cumulative Layout Shift | < 0.1 |
| FCP | First Contentful Paint | < 1.8초 |
| TTFB | Time to First Byte | < 800ms |
| TBT | Total Blocking Time | < 200ms |

---

## 6. PWA & Service Worker 확인

- [ ] `public/manifest.json` — `name`, `short_name`, `theme_color`, `display: standalone`
- [ ] DevTools → Application → Manifest → "Add to homescreen" 클릭 가능
- [ ] DevTools → Application → Service Workers → **activated and running** 상태
- [ ] iOS Safari → 공유(□↑) → 홈 화면에 추가 → standalone 모드 실행

### 캐시 스토리지 확인

- [ ] `workbox-precache-v2-*` → 정적 파일 precache 등록
- [ ] API 응답 NetworkFirst 캐시 동작 확인
- [ ] 이미지 CacheFirst 캐시 동작 확인 (7일)

---

## 7. 오프라인 동작 테스트

DevTools → Network 탭 → **Offline** 체크:

- [ ] `/library` → 이전 캐시 데이터 표시
- [ ] `/stats` → 캐시된 차트 표시
- [ ] 새 API 쓰기 요청 → 네트워크 에러 UI 표시
- [ ] 온라인 복귀 → 자동 재요청 + 데이터 갱신

## 7-1. 반응형/디바이스 회귀 (신규)

- [ ] iPhone Safari(노치 기기)에서 TopBar가 상태바/노치와 겹치지 않는지 확인
- [ ] iPhone Safari에서 BottomNav와 콘텐츠가 홈 인디케이터와 겹치지 않는지 확인
- [ ] Android Chrome에서 주소창 접힘/펼침 시 레이아웃 점프가 과도하지 않은지 확인
- [ ] 세로/가로 회전 시 `BookDetailPage`, `NotesSearchPage`, `RegisterFlowPage` 높이 계산이 깨지지 않는지 확인

---

## 8. 접근성 체크

- [ ] 모든 이미지에 `alt` 속성
- [ ] 버튼/아이콘에 `aria-label` 적용
- [ ] 색상 대비 ≥ 4.5:1 (WCAG AA)
- [ ] Tab 키 네비게이션 논리적 순서
- [ ] `:focus-visible` 포커스 링 표시

---

## 9. 최종 판정 기준

| 검사 항목 | 목표 | 결과 |
|-----------|------|------|
| E2E 자동 테스트 | 27/27 PASS | |
| 초기 번들 크기 | < 600 kB gzip | |
| Lighthouse Performance | ≥ 80 | |
| Lighthouse Accessibility | ≥ 90 | |
| Lighthouse PWA | ≥ 90 | |
| LCP | < 2.5초 | |
| CLS | < 0.1 | |
| PWA 설치 동작 | Pass | |
| 오프라인 캐시 동작 | Pass | |

---

## 트러블슈팅

### Performance 점수 낮음

```bash
npx vite-bundle-visualizer
```
- `vendor-charts` lazy 분리 확인 (`/stats` 직접 접속 시에만 로드)
- `framer-motion` 트리쉐이킹: `import { motion } from 'framer-motion/m'` 검토

### PWA 점수 낮음
- HTTPS 필수: localhost는 OK, 프로덕션은 Workers 자동 HTTPS
- SW 등록 실패: `vite.config.ts`의 `devOptions.enabled: true` 확인

### LCP 개선
- 첫 렌더 hero 이미지: `loading="eager"` + `fetchpriority="high"`
- `BookCover` 컴포넌트 고정 비율 컨테이너 확인

### CLS 개선
- 이미지에 명시적 `width`/`height` 또는 `aspect-ratio` CSS 적용
